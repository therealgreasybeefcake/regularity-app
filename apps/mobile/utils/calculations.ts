// Calculations now live in @regularity/core (pure TypeScript, shared with the
// API and web live-view). Re-exported here so existing relative imports
// (`../utils/calculations`, `../../utils/calculations`) keep working unchanged.
export * from '@regularity/core';
